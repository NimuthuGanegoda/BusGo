import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ── Primary / Background ──────────────────────────────────────────
  static const Color primary      = Color(0xFF0A1628);   // deep navy
  static const Color secondary    = Color(0xFF4ECDC4);   // teal accent
  static const Color danger       = Color(0xFFE74C3C);
  static const Color warning      = Color(0xFFF39C12);
  static const Color success      = Color(0xFF2ECC71);

  // ── Surfaces ──────────────────────────────────────────────────────
  static const Color surface      = Color(0xFF040A14);   // scaffold bg
  static const Color card         = Color(0xFF0A1628);   // card bg
  static const Color textMuted    = Color(0xFF6B7A8D);
  static const Color border       = Color(0x1AFFFFFF);   // 10% white
  static const Color divider      = Color(0x1AFFFFFF);

  // ── Accent colors ─────────────────────────────────────────────────
  static const Color starFilled   = Color(0xFFF39C12);
  static const Color starEmpty    = Color(0xFF333333);
  static const Color lightBlue    = Color(0xFF4ECDC4);   // teal for subtitle
  static const Color lightBlue2   = Color(0xFF7EDDD7);
  static const Color inputBg      = Color(0xFF1A1E2E);
  static const Color headerBg     = Color(0xFF0A1628);
  static const Color iconBg       = Color(0x1A4ECDC4);   // 10% teal
  static const Color textPrimary  = Color(0xFFFFFFFF);
}










