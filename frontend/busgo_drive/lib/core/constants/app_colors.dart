import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ── Primary (dark theme base) ─────────────────────────────────────────
  static const Color primary       = Color(0xFF000000);
  static const Color primaryLight  = Color(0xFF4ECDC4);  // teal accent
  static const Color primaryDark   = Color(0xFF000000);

  // ── Accent (teal from CodePen) ────────────────────────────────────────
  static const Color accent        = Color(0xFF4ECDC4);
  static const Color accentLight   = Color(0xFF7EDDD7);
  static const Color accentDark    = Color(0xFF36B5AD);

  // ── Status colors ─────────────────────────────────────────────────────
  static const Color success       = Color(0xFF2ECC71);
  static const Color successLight  = Color(0x1A2ECC71);
  static const Color warning       = Color(0xFFF39C12);
  static const Color warningLight  = Color(0x1AF39C12);
  static const Color danger        = Color(0xFFE74C3C);
  static const Color dangerLight   = Color(0x1AE74C3C);
  static const Color info          = Color(0xFF3498DB);
  static const Color infoLight     = Color(0x1A3498DB);

  // ── Backgrounds & surfaces (dark glass) ───────────────────────────────
  static const Color background    = Color(0xFF000000);
  static const Color surface       = Color(0xFF1E1E1E);
  static const Color cardBg        = Color(0xCC1E1E1E);  // rgba(30,30,30,0.8)
  static const Color divider       = Color(0x1AFFFFFF);   // rgba(255,255,255,0.1)
  static const Color border        = Color(0x1AFFFFFF);
  static const Color inputBg       = Color(0xFF1E1E1E);

  // ── Text ──────────────────────────────────────────────────────────────
  static const Color textPrimary   = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFAAAAAA);
  static const Color textMuted     = Color(0xFF777777);
  static const Color textOnPrimary = Color(0xFFFFFFFF);
  static const Color textOnDark    = Color(0xFF4ECDC4);

  // ── Status indicators ─────────────────────────────────────────────────
  static const Color online        = Color(0xFF2ECC71);
  static const Color offline       = Color(0xFFE74C3C);
  static const Color tripActive    = Color(0xFF4ECDC4);

  // ── SOS ───────────────────────────────────────────────────────────────
  static const Color sosRed        = Color(0xFFE74C3C);
  static const Color sosRedBg      = Color(0x1AE74C3C);

  // ── Map ───────────────────────────────────────────────────────────────
  static const Color mapRoute      = Color(0xFF4ECDC4);
  static const Color mapBus        = Color(0xFF4ECDC4);
  static const Color mapStop       = Color(0xFFE74C3C);
  static const Color mapUser       = Color(0xFF2ECC71);

  // ── Ratings ───────────────────────────────────────────────────────────
  static const Color starFilled    = Color(0xFFF39C12);
  static const Color starEmpty     = Color(0xFF333333);
}








